{
  "name": "storm",
  "version": "0.8.0",
  "type": "module",
  "private": true,
  "description": "Storm — multi-engine council for Claude Code (plan mode)",
  "scripts": {
    "test": "node --test"
  }
}
