{
  "name": "storm",
  "version": "0.12.0",
  "type": "module",
  "private": true,
  "description": "Storm — multi-engine council + single-engine delegation for Claude Code",
  "scripts": {
    "test": "node --test"
  }
}
